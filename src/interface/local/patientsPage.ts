import { head } from "@/interface/head";

export interface localizationPatientsPage {
    head: head,
    bannerText: string;
    title: string;
    file_name: string;
    find: string;
    upload_date: string;
    download: string;
    download_all: string;
}